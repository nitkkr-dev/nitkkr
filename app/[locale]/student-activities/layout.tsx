// Student activities - club info updates occasionally
export const revalidate = 300;

export default function StudentActivitiesLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
