'use client';

import Loading from '~/components/loading';

export default function Page() {
  return (
    <main className="global container grid h-dvh place-items-center">
      <Loading />
    </main>
  );
}
